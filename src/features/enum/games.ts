export enum GameStatusEnum {
    OPEN = "open",
    CLOSED = "closed",
    CANCELED = "canceled",
    COMPLETED = "completed",
}

export enum ParticipationStatusEnum {
    REQUESTED = "requested",
    CANCELED = "canceled",
    WITHDRAWN = "withdrawn",
    CONFIRMED = "confirmed",
}